{
  "name": "thecruxco",
  "type": "module",
  "version": "0.1.0",
  "description": "TheCruxCo — daily knowledge-first news digest",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "11",
    "@astrojs/rss": "^4.0.11",
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
